# Copy this file to .env for local development.
# NEVER commit .env or real credentials.

# Use a generic default in the public repository. Set your actual local
# timezone in .env if/when a service needs it.
TZ=Etc/UTC

# Additional service-specific settings will be added as integrations are
# introduced. Keep credentials and environment-specific hostnames out of this
# example file.
